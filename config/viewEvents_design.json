{
  "_id": "_design/viewEvents",
  "_rev": "5-53af92a335346d6b2065ec8c676fe6db",
  "language": "javascript",
  "updates": {
    "log_tab_tap": "function(doc, req) {\n      if (!doc || doc._id === '_design/viewEvents') {\n        return [null, 'no doc'];\n      }\n      \n      var body = {};\n      if (req.body) {\n        try { body = JSON.parse(req.body); } catch(e) {}\n      }\n      \n      // Use test_status from request if provided, otherwise use from database\n      var testStatus = body.test_status || doc.status || doc.test_status || 'unknown';\n      \n      var event = {\n        viewer: body.viewer || req.userCtx.name || 'anonymous',\n        session_id: body.session_id || req.query.session_id || 'unknown',\n        test_status: testStatus,\n        tab_status: body.tab_status || 'details',\n        timestamp: new Date().toISOString()\n      };\n      \n      if (!doc.view_events) doc.view_events = [];\n      doc.view_events.push(event);\n      \n      return [doc, JSON.stringify({ok: true, event: event})];\n    }"
  }
}
